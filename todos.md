# TODOS:

1. Update tmux sessionizer script to do the following:
    - When opening a project, open the first pane in neovim at the base of the project
    ```bash
    # Should just have to run the following command
    nvim .
    ```
    - Open a second pane in just the terminal
    - Check if the project has a git repository and if it does, then open a third pane with lazygit
    ```bash
    # Should just have to run the following command
    lazygit
    ```
    
